pip install tensorflow numpy matplotlib picamera2
sudo apt update
sudo apt install libcap-dev -y
sudo apt install build-essential python3-dev -y
mkdir -p data/pre_harvest
mkdir -p data/harvest
mkdir -p data/post_harvest
source /path/to/your/env/bin/activate
geany
git clone https://github.com/raspberrypi/libcamera.git
cd libcamera
git checkout --recurse-submodules v0.3.x
./build.py -p python
sudo python3 setup.py install
